## Packages
date-fns | Date formatting for calendars and due dates
framer-motion | Smooth animations for page transitions and card interactions
recharts | Charts for student progress visualization
react-day-picker | Calendar widget
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
The app uses a specific blue (#003366) and yellow accent color scheme.
Dashboard requires a 3-column layout (Sidebar, Main Content, Right Widgets).
